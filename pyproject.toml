[project]
name = "pmpp-gpu-course"
version = "0.1.0"
description = "Self-study CUDA course scaffolding for Programming Massively Parallel Processors."
requires-python = ">=3.11"
dependencies = [
    "modal>=1.1.0",
]

[tool.uv]
package = false

